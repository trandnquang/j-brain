package com.trandnquang.j_brain.domain.enums;

/**
 * Search mode selector — drives which Jotoba endpoint is called
 * and which React tab is rendered.
 */
public enum SearchMode {
    WORD,
    KANJI,
    SENTENCE,
    NAME
}
